# Core — statistical and data
pandas>=2.2
tqdm>=4.66
numpy>=2.0
scipy>=1.13
statsmodels>=0.14
scikit-learn>=1.5
patsy>=0.5
pyarrow>=16.0          # parquet engine for data/processed

# Visualisation
matplotlib>=3.9
seaborn>=0.13

# Notebooks
jupyterlab>=4.2
ipykernel>=6.29

# GNN — install torch first from https://pytorch.org then:
# pip install torch-geometric
torch>=2.4
torch-geometric>=2.6

# Dataset download
kaggle>=1.6

# Tabular model bake-off (scripts/02_model_benchmark.py) — multi-model CV comparison.
xgboost>=2.1
lightgbm>=4.6.0       # >=4.6.0: CVE-2024-43598 (RCE via untrusted model load) fix
# tabpfn>=2.0          # optional; not currently called

# Optional — GPU only, requires CUDA 12+ (replaces the CPU fixed-A SSM fallback)
# pip install mamba-ssm causal-conv1d
